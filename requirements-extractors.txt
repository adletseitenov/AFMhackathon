# Опциональные ТЯЖЁЛЫЕ экстракторы для РЕАЛЬНОГО мультимодального live-пути (разбор видео).
# Ядро КӨЗ работает и без них (кэш-признаки + текстовый анализ Telegram/ссылок).
# Ставить для настоящего Whisper (аудио), EasyOCR (текст с кадров), open-CLIP (визуал).
#
# torch ставить CPU-сборкой (иначе тянет CUDA ~2.5 ГБ):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
torch
torchvision
faster-whisper
easyocr
open-clip-torch
opencv-python-headless
imageio-ffmpeg
curl_cffi
# curl_cffi нужен yt-dlp для impersonation TikTok (иначе "Unable to extract universal data").
# Плюс нужен статичный ffmpeg+ffprobe (yt-dlp использует для нарезки/склейки).
# Windows: https://github.com/BtbN/FFmpeg-Builds/releases -> положить ffmpeg.exe/ffprobe.exe
# в C:\Users\<user>\AppData\Local\Programs\ffmpeg\bin (app.ingestion.fetch._ffmpeg_location ищет там).
