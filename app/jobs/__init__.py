"""Фоновая очередь задач КӨЗ.

Чтобы ~30-секундный CPU-анализ видео не блокировал API/UI, тяжёлая работа
выполняется в пуле потоков (worker.py), а её прогресс/результат хранятся
в потокобезопасном in-memory реестре (store.py) и отдаются по /api/jobs/{id}
(routes.py). Любая ошибка внутри задачи фиксируется как status='error' и
НИКОГДА не всплывает 500-м (R3).
"""
