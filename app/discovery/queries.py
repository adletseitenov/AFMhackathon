"""Скам-запросы RU/KZ для автономного поиска опасного контента в интернете.

ФОЛЛБЭК-список: единый плоский набор всех скам-запросов. С введением
НАСТРАИВАЕМОГО автопоиска источник истины — реестр app/discovery/catalog.py
(страны/категории/типы/сортировки). Этот список собирается из объединения всех
категорий каталога и остаётся как фоллбэк: discover() использует его, когда
явные queries не переданы И не задана фильтрация по стране/категориям.

Подобран под профиль АФМ: нелицензированные онлайн-казино/букмекеры, финпирамиды,
гарантированный доход, реф-схемы, крипто-удвоение, "пассивный доход", "хайп-проекты".
Латиница (KZ) добавлена намеренно — выдача на казахском часто индексируется так.
"""

from app.discovery.catalog import build_queries

# Объединение запросов ВСЕХ категорий каталога (без гео-привязки) — плоский
# фоллбэк-список. Реестр catalog.py — единая точка расширения.
DISCOVERY_QUERIES = build_queries(country="all", categories=None)
